
// Global client instance. You can hit it in your browser console (window.client)
var client = null;

// When the DOM is ready, let's party
$(document).ready(function(){

    // Create a new client instance
    client = new Client();

});